export const RequiredString = {
  type: String,
  required: true,
};

export const RequiredNumber = {
  type: Number,
  required: true,
};

export const RequiredBoolean = {
  type: Boolean,
  required: true,
};
